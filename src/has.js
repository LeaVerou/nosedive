/**
* Does the given object have the given path?
* Faster than `get()` for this use case.
* @param {object} obj
* @param {(string | number | object)[]} path
* @returns {boolean} Whether the path exists
*/
export default function has (obj, path) {
	// TODO implement this
	return true;
}